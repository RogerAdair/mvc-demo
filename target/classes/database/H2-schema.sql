CREATE TABLE IF NOT EXISTS t_user (user_id integer GENERATED BY DEFAULT AS IDENTITY, created_at TIMESTAMP NOT NULL,  user_stu_id varchar(255) NOT NULL, user_name varchar(255) NOT NULL, user_score integer,user_password varchar(255) NOT NULL, updated_at TIMESTAMP NOT NULL, VERSION integer NOT NULL, PRIMARY KEY (user_id))
/*ALTER TABLE t_user ADD CONSTRAINT uk_user_email UNIQUE (user_email)*/